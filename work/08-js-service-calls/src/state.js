// Define a state object to hold the application state

const state = {
    username: '', // Current logged-in username
    storedWord: '', // Word stored for the logged-in user
    error: '', // Error message to display
};

export default state;